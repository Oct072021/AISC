# `<%- packageJson.name %>`审计结果

> **审计环境**：Node <%- options.nodeVersion %> / npm <%- options.npmVersion %>
<% if(options.incompatible) { %>

> **⚠ 兼容性警告**：项目的 `package-lock.json` 使用 lockfileVersion <%- options.lockfileVersion %>（需要 npm 7+），但当前环境的 npm 版本为 <%- options.npmVersion %>，不兼容。已回退为使用当前环境重新生成依赖树，审计结果可能与项目实际依赖存在偏差。
<% } %>
<% if(!options.reused && !options.incompatible) { %>

> **注意**：当前项目未检测到 lock 文件，本次审计使用 Node <%- options.nodeVersion %> 环境生成依赖树。若该版本与项目实际使用的 Node 版本不一致，审计结果可能存在偏差。建议在项目中安装并提交 lock 文件或切换 Node 版本后重新执行审计。
<% } %>

<% if(audit.summary.total) { %>
<%- include('./audit.ejs'); %>
<% } %>
<% if(audit.summary.total === 0) { %>
你项目的所有直接依赖和间接依赖都没有发现任何风险漏洞。
<% } %>
